# Rangos, no versiones exactas: la AMI de la EC2 puede traer un Python mas nuevo
# que los wheels de una version fija, y pip terminaria compilando numpy/scipy
# desde fuente en una t2.micro. Con rangos, pip escoge el wheel que si existe
# para el interprete que haya en la maquina.
Flask>=3.0,<4
gunicorn>=21.2
pandas>=2.2
numpy>=1.26
scipy>=1.11
requests>=2.31
python-dateutil>=2.9
